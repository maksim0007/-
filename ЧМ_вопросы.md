## 1.Приближенные числа. Погрешности вычисления. Значащие и верные цифры числа.
К приближенным относятся числа, близкие к истинному значению.Погрешностью  приближенного числа называется разность между точным значением числа x и его приближенным значением.Значащая цифра — это цифра в записи числа, которая несёт информацию о его величине начиная с первой ненулевой слева.Цифра в приближённом десятичном числе считается верной в широком смысле, если абсолютная погрешность не больше одной единицы соответствующего разряда, и верной в узком (строгом) смысле, если абсолютная погрешность не больше половины единицы этого разряда.
## 2. Погрешности арифметических операций - погрешности суммы, разности, произведения, частного приближенных чисел.
Пусть $|\Delta x|, |\Delta y|$ — абсолютные погрешности.

- Сумма/разность: $|\Delta(x\pm y)| \le |\Delta x| + |\Delta y|$.

- Произведение: $|\Delta(xy)| \le |y||\Delta x| + |x||\Delta y| \ (+|\Delta x||\Delta y|)$.

- Частное: $\left|\Delta\!\left(\frac{x}{y}\right)\right| \approx \left|\frac{\Delta x}{y}\right| + \left|\frac{x\,\Delta y}{y^2}\right|$, при $y\neq 0$.

- В относительных: для $\times$ и $/$ обычно $\delta \approx \delta_x + \delta_y$.
## 3. Представление чисел в ЭВМ. Понятия машинного эпсилон, машинной
бесконечности, машинного нуля.
- Представление чисел в ЭВМ (плавающая точка):  
  $$
  x=\pm m\cdot \beta^{e},
  $$
  где $m$ — мантисса, $e$ — порядок, $\beta$ — основание (обычно $2$).

- Машинный эпсилон $\varepsilon_{\text{mach}}$: минимальное $\varepsilon>0$, такое что  
  $$
  1+\varepsilon>1
  $$
  в машинной арифметике.

- Машинная бесконечность: переполнение (overflow), когда $|x|>x_{\max}$, результат становится $\pm\infty$ (или ошибка переполнения).

- Машинный нуль: underflow/округление к нулю, когда $|x|$ меньше минимального представимого положительного числа и значение превращается в $0$.
## 4. Вычислительные задачи. Корректность и обусловленность вычислительных задач.

Вычислительная задача — это задача вида: по заданным исходным данным $x$ найти результат $y$
Задача **корректна**, если выполняются 3 условия:
1) **Существование** решения.
2) **Единственность** решения.
3) **Устойчивость**: малое изменение данных вызывает малое изменение 

**Обусловленность** — это чувствительность решения к погрешностям исходных данных.

## 5. Прямая и обратная задачи теории погрешностей.
#### Прямая задача (теория погрешностей)
Дано: приближённые входные данные $x$ и их погрешности $\Delta x$ (или $|\Delta x|$, $\delta x$).  
Найти: погрешность результата $y=F(x)$, то есть $\Delta y$ (или оценку $|\Delta y|$, $\delta y$).

Идея: «как ошибка входа проходит через вычисления».
#### Обратная задача (теория погрешностей)
Дано: требуемая точность результата $y$ (например, $|\Delta y|\le \varepsilon$).  
Найти: какую точность надо обеспечить во входных данных $x$, то есть ограничения на $\Delta x$ (или требуемое число верных/значащих цифр).

Идея: «какой точности входа достаточно, чтобы ответ был нужной точности».

## 6. Прямые методы решения СЛАУ. Метод Гаусса: основная идея и схемы реализации (схема единственного деления и с выбором главных элементов).
## Прямые методы решения СЛАУ
Прямые методы — алгоритмы, которые находят решение $Ax=b$ за конечное число шагов (в точной арифметике)

- **Метод Гаусса (идея)**:
  - На первом этапе последовательно исключают неизвестные, превращая систему в эквивалентную, но более простую (обычно с треугольной матрицей).
  - На втором этапе находят неизвестные обратно: сначала последнюю, потом предпоследнюю и т.д.

- **Схема единственного деления**: это тот же Гаусс, но организованный так, чтобы делений было меньше (деление стараются выполнить один раз на шаг, а дальше используют умножение/вычитание).

- **Гаусс с выбором главных элементов**: перед исключением на каждом шаге специально выбирают «лучший» ведущий элемент (обычно самый большой по модулю), переставляя строки (иногда и столбцы). Это делает метод устойчивее и уменьшает ошибки округления, а также помогает избежать деления на ноль или на очень маленькое число.

## 7. Метод прогонки решения систем линейных алгебраических уравнений с трехдиагональной матрицей. 
## Метод прогонки (Thomas algorithm)
Метод прогонки — это прямой метод для решения СЛАУ, у которых матрица **трёхдиагональная** (ненулевые элементы только на главной диагонали и двух соседних).

### Идея
- Использует то, что в системе уравнения «связаны» только с соседними неизвестными, поэтому можно решить быстро, без полного Гаусса.
- Работает в 2 прохода по системе:
  1) **Прямой ход (прогонка вперёд):** последовательно упрощают уравнения, исключая неизвестные так, чтобы каждое следующее уравнение зависело от всё меньшего числа переменных.
  2) **Обратный ход (прогонка назад):** находят неизвестные в обратном порядке, начиная с последней, подставляя назад.


## 8. Нормы векторов и матриц. Обусловленность задачи решения СЛАУ. Число обусловленности.
###  Нормы
- **Норма вектора** — мера “длины” вектора .
- **Норма матрицы** — мера того, насколько матрица “растягивает” векторы .

### Обусловленность СЛАУ, Число обусловленности
Обусловленность задачи $Ax=b$ — чувствительность решения $x$ к малым ошибкам в $A$ и/или $b$.
$$
\mathrm{cond}(A)=\|A\|\cdot\|A^{-1}\|.
$$
Чем больше $\mathrm{cond}(A)$, тем хуже обусловлена задача (тем сильнее ошибки данных портят ответ).

## 9. Приближенное решение систем линейных уравнений: возмущение правой части, число обусловленности матрицы и погрешность решения
### Приближённое решение СЛАУ: возмущение правой части
Пусть решаем $Ax=b$, но правая часть известна с ошибкой:
$$
b \to b+\Delta b.
$$
Тогда решение тоже меняется:
$$
x \to x+\Delta x.
$$

### Связь с числом обусловленности
Чувствительность решения к ошибке в $b$ оценивают через число обусловленности матрицы:
$$
\mathrm{cond}(A)=\|A\|\cdot\|A^{-1}\|.
$$

### Оценка погрешности решения (смысл)
- Если $\mathrm{cond}(A)$ **малое**, то ошибка в $b$ даёт примерно такую же (по порядку) относительную ошибку в $x$.
- Если $\mathrm{cond}(A)$ **большое**, то даже маленькая ошибка в $b$ может сильно “раздуть” ошибку в решении.

Типичная оценка относительной погрешности:
$$
\frac{\|\Delta x\|}{\|x\|}\ \lesssim\ \mathrm{cond}(A)\,\frac{\|\Delta b\|}{\|b\|}.
$$
## 10-12. Итерационные методы СЛАУ: Якоби и Зейдель (суть), условие сходимости

### Общая идея
- Не решают $Ax=b$ сразу, а строят приближения $x^{(k)}$.
- На каждом шаге пересчитывают неизвестные из уравнений системы, пока ответ не “стабилизируется” (невязка/изменение малые).

### Метод Якоби (суть)
- На шаге $k+1$ **все неизвестные пересчитываются по старому вектору** $x^{(k)}$.
- То есть новые значения не влияют друг на друга внутри одного шага: сначала посчитали весь $x^{(k+1)}$, потом переход.

### Метод Зейделя (суть)
- На шаге $k+1$ неизвестные пересчитываются **по очереди**, и каждое новое значение **сразу используется** при вычислении следующих.
- То есть внутри одного шага уже происходит “уточнение” за счёт свежих компонент.
### Схоидмость
- Метод Якоби и Зейделя сходится, если матрица системы $A$ имеет **диагональное преобладание**.

## 13-15. Метод простых итераций для приближённого решения СЛАУ

### Идея
СЛАУ $Ax=b$ переписывают в эквивалентный вид:
$$
x = Bx + c,
$$
и затем строят последовательность приближений.

### Алгоритм
1) Привести систему к виду $x=Bx+c$ (выбрать $B,c$).
2) Задать начальное приближение $x^{(0)}$.
3) Повторять:
$$
x^{(k+1)} = Bx^{(k)} + c,
$$
### Достаточное условие
$$
\|B\| < 1
$$


### Необходимое и достаточное условие
$$
\rho(B) < 1,
$$
где $\rho(B)$ — спектральный радиус (максимум модулей собственных значений $B$).


## 16. Функции MATHEMATICA для решения задач линейной алгебры
**FindRoot[ {eqn1, eqn2, …, eqnn}, {x1, x1^(0)}, {x2, x2^(0)}, …, {xn, xn^(0)} ]** — находит численное решение системы n уравнений eqn1, eqn2, …, eqnn с n неизвестными x1, x2, …, xn, если задано начальное приближение (x1^(0), x2^(0), …, xn^(0)).

**LinearSolve[ A, B ]** — находит решение системы линейных алгебраических уравнений вида AX = B.

**NSolve[ {eqn1, eqn2, …}, {x1, x2, …} ]** — находит численное решение заданной системы уравнений.

**NSolve[ {eqn1, eqn2, …}, {x1, x2, …}, Reals ]** — находит численное решение заданной системы уравнений на множестве действительных чисел.

**Solve[ {eqn1, eqn2, …}, {x1, x2, …} ]** — находит решение системы уравнений eqn1, eqn2, … относительно переменных x1, x2, …

## 17. Интерполирование функции: постановка и общее решение полиномом

### Постановка задачи
Даны узлы $x_0,x_1,\dots,x_n$ и значения функции в них $f(x_0),f(x_1),\dots,f(x_n)$.  
Найти полином $P_n(x)$ степени не выше $n$, такой что
$$
P_n(x_i)=f(x_i), \quad i=0,1,\dots,n.
$$

### Общее решение (полином интерполяции)
- Решение существует и единственно (если все узлы $x_i$ различны).
- Интерполяционный полином можно записать в стандартных формах:
  - форма Лагранжа;
  - форма Ньютона (по разделённым разностям).


## 18. Интерполяционная формула Лагранжа
$$
P_n(x)=\sum_{i=0}^{n} f(x_i)\,L_i(x),
$$
где
$$
L_i(x)=\prod_{\substack{j=0\\ j\ne i}}^{n}\frac{x-x_j}{x_i-x_j}.
$$

### Погрешность интерполяционной формулы

$$
R_n(x)=\frac{f^{(n+1)}(\xi)}{(n+1)!}\prod_{i=0}^{n}(x-x_i), \quad \xi\in[a,b].
$$
## 19. Разделённые разности

### 1-го порядка
Для двух узлов $x_i\ne x_{i+1}$:
$$
f[x_i,x_{i+1}]=\frac{f(x_{i+1})-f(x_i)}{x_{i+1}-x_i}.
$$

### Высших порядков
Разделённая разность порядка $k$:
$$
f[x_i,x_{i+1},\dots,x_{i+k}]
=\frac{f[x_{i+1},\dots,x_{i+k}]-f[x_i,\dots,x_{i+k-1}]}{x_{i+k}-x_i}.
$$

## Таблица разделённых разностей (схема)
![alt text](image.png)

## 20. Интерполяционные формулы Ньютона через разделённые разности

Пусть заданы узлы $x_0,x_1,\dots,x_n$ (все различны) и значения $y_i=f(x_i)$.
### Ньютон “в начале таблицы” (узлы в порядке $x_0,x_1,\dots$)
$$
P_n(x)=f[x_0]+f[x_0,x_1](x-x_0)+f[x_0,x_1,x_2](x-x_0)(x-x_1)+\dots
$$
$$
\dots+f[x_0,\dots,x_n]\prod_{j=0}^{n-1}(x-x_j).
$$

### Ньютон “в конце таблицы” (узлы в порядке $x_n,x_{n-1},\dots$)
Если упорядочить узлы наоборот, то:
$$
P_n(x)=f[x_n]+f[x_n,x_{n-1}](x-x_n)+f[x_n,x_{n-1},x_{n-2}](x-x_n)(x-x_{n-1})+\dots
$$
$$
\dots+f[x_n,\dots,x_0]\prod_{j=0}^{n-1}\bigl(x-x_{n-j}\bigr).
$$

## 21. Погрешности интерполяционных формул

Пусть $P_n(x)$ — интерполяционный многочлен степени $\le n$ по узлам $x_0,\dots,x_n$.

### Остаточный член (погрешность)
$$
R_n(x)=f(x)-P_n(x).
$$

Если $f\in C^{n+1}[a,b]$, то
$$
R_n(x)=\frac{f^{(n+1)}(\xi)}{(n+1)!}\prod_{i=0}^{n}(x-x_i),\quad \xi\in(a,b).
$$

## 22!!!. Интерполяция кусочно-многочленами. Сплайны. Кубический сплайн

### Кусочно-многочленная интерполяция
На каждом отрезке $[x_i,x_{i+1}]$ функцию приближают своим многочленом $P_i(x)$, а на всём $[x_0,x_n]$ получается “склейка” этих многочленов.

### Сплайн
Сплайн степени $m$ — это кусочно-многочленная функция, у которой в узлах $x_i$ обеспечена гладкость (обычно непрерывность производных до порядка $m-1$).

### Интерполяционный кубический сплайн
Кубический сплайн $S(x)$ строится так:
- На каждом $[x_i,x_{i+1}]$:
$$
S_i(x)=a_i+b_i(x-x_i)+c_i(x-x_i)^2+d_i(x-x_i)^3.
$$


## 23. Аппроксимация МНК. Точечная и интегральная

### Идея метода наименьших квадратов (МНК)
Ищем многочлен $p_m(x)=a_0+a_1x+\dots+a_mx^m$, который минимизирует “суммарную квадратичную ошибку”.

### Точечная (дискретная) аппроксимация
Даны точки $(x_i,y_i)$, $i=1,\dots,N$. Минимизируем
$$
Q(a_0,\dots,a_m)=\sum_{i=1}^{N}\bigl(p_m(x_i)-y_i\bigr)^2.
$$
Условия минимума (нормальные уравнения):
$$
\frac{\partial Q}{\partial a_k}=0,\quad k=0,\dots,m.
$$

### Интегральная аппроксимация
На $[a,b]$ минимизируем
$$
J(a_0,\dots,a_m)=\int_a^b\bigl(p_m(x)-f(x)\bigr)^2\,dx
$$

## 24. Равномерное приближение. Вейерштрасс. Тейлор. Альтернанс (не нашел в сэо).

### Равномерное приближение
Говорят, что $p_n$ равномерно приближает $f$ на $[a,b]$, если
$$
\|f-p_n\|_\infty=\max_{x\in[a,b]}|f(x)-p_n(x)|\to 0.
$$

### Теорема Вейерштрасса
Для любой непрерывной $f$ на $[a,b]$ и любого $\varepsilon>0$ существует многочлен $p(x)$ такой, что
$$
\max_{x\in[a,b]}|f(x)-p(x)|<\varepsilon.
$$

### Равномерное приближение многочленом Тейлора
Если $f\in C^{n+1}[a,b]$, то для разложения в точке $a$:
$$
f(x)=T_n(x)+R_n(x),
$$
$$
R_n(x)=\frac{f^{(n+1)}(\xi)}{(n+1)!}(x-a)^{n+1},\quad \xi\in(a,b).
$$
Если $|f^{(n+1)}(x)|\le M$ на $[a,b]$, то
$$
\max_{x\in[a,b]}|R_n(x)|\le \frac{M}{(n+1)!}(b-a)^{n+1}.
$$

### Теорема Чебышева об альтернансе
Многочлен $p_n^*$ степени $\le n$ является наилучшим равномерным приближением $f$ на $[a,b]$
тогда и только тогда, когда существуют $n+2$ точки
$$
a\le t_0<t_1<\dots<t_{n+1}\le b,
$$
в которых ошибка достигает максимума по модулю и меняет знак:
$$
f(t_k)-p_n^*(t_k)=(-1)^k\,E,\quad E=\|f-p_n^*\|_\infty.
$$
## 25. Функции MATHEMATICA для аппроксимации функций (как в файле)

- `InterpolatingPolynomial[data, x]` — строит интерполяционный многочлен по данным `data`.
- `Interpolation[data]` — строит интерполяционную функцию `InterpolatingFunction`.
- `Interpolation[data, Method -> "Spline"]` — сплайн-интерполяция 

- `SplineFit[data, type]` — аппроксимация/интерполяция сплайном 


- `Fit[data, {f1, f2, …, fm}, x]` — аппроксимация по МНК в заданном базисе.
  (в файле базис вида `{1, x, …, x^m}`).


## 26. Приближённое решение уравнений

Решают уравнение в виде $f(x)=0$ (алгебраическое или трансцендентное).

### Отделение (изоляция) корней
**Аналитическое** (по знаку и свойствам):
- Найти промежуток $[a,b]$, где $f$ непрерывна и $f(a)\,f(b)<0$ ⇒ на $(a,b)$ есть хотя бы один корень.
- Если дополнительно $f'(x)$ не меняет знак на $(a,b)$ (монотонность), то корень на этом интервале единственный.

**Графическое**:
- Построить график $y=f(x)$ и найти пересечения с осью $Ox$ (точки, где $y=0$).
- Для уравнения $g(x)=h(x)$ строят $y=g(x)$ и $y=h(x)$; точки пересечения дают корни.

### Теорема об оценке погрешности приближённого корня
Если $f\in C^1[a,b]$, $x^*$ — точный корень, а на $[a,b]$ выполнено
$$
|f'(x)|\ge m>0,
$$
то для любого приближения $\tilde x\in[a,b]$:
$$
|\tilde x-x^*|\le \frac{|f(\tilde x)|}{m}.
$$
## 27. Метод половинного деления и метод хорд

Решаем уравнение $f(x)=0$.

### Метод половинного деления (биссекции)
**Условия:** $f$ непрерывна на $[a,b]$ и $f(a)\,f(b)<0$.

**Алгоритм:**
1) $c=\dfrac{a+b}{2}$.
2) Если $f(a)f(c)<0$, то $b:=c$, иначе $a:=c$.
3) Повторять, пока $b-a<\varepsilon$ (или $|f(c)|<\varepsilon$).

**Оценка погрешности после $n$ шагов:**
$$
|x^*-c_n|\le \frac{b-a}{2^n}.
$$

### Метод хорд (секущих)
Берут два начальных приближения $x_0, x_1$.

**Итерационная формула:**
$$
x_{k+1}=x_k-\;f(x_k)\,\frac{x_k-x_{k-1}}{f(x_k)-f(x_{k-1})},\quad k\ge 1.
$$

**Свойства:**
- Не требует вычисления производной.
- Обычно сходится быстрее биссекции, но сходимость не гарантируется без удачных начальных $x_0,x_1$.
## 28. Метод Ньютона 

Решаем уравнение $f(x)=0$.

### Итерационная формула
При заданном начальном приближении $x_0$:
$$
x_{k+1}=x_k-\frac{f(x_k)}{f'(x_k)},\quad k=0,1,2,\dots
$$
 Выполняют до момента 
- $|x_{k+1}-x_k|<\varepsilon$,
- $|f(x_{k+1})|<\varepsilon$.
### Условия сходимости (достаточные, типовые)
- $f$ непрерывно дифференцируема (обычно $f\in C^2$) в окрестности корня $x^*$.
- $f'(x^*)\ne 0$ (простой корень).
- Начальное $x_0$ достаточно близко к $x^*$.
 ## 30. Примеры одношагового и многошагового методов приближенного решения нелинейного уравнения.

### Одношаговые (используют только $x_k$)
- **Метод простой итерации**:
$$
x_{k+1}=\varphi(x_k).
$$
- **Метод Ньютона**:
$$
x_{k+1}=x_k-\frac{f(x_k)}{f'(x_k)}.
$$

### Многошаговые (используют несколько прошлых приближений)
- **Метод хорд (секущих)**:
$$
x_{k+1}=x_k-\;f(x_k)\,\frac{x_k-x_{k-1}}{f(x_k)-f(x_{k-1})},\quad k\ge1.
$$
- **Метод  хорд**:
если на $[a_k,b_k]$ $f(a_k)f(b_k)<0$, то
$$
x_{k+1}=\frac{a_k f(b_k)-b_k f(a_k)}{f(b_k)-f(a_k)},
$$
 ## 31. Достаточное условие сходимости метода простой итерации

Решаем $f(x)=0$ в виде фиксированной точки:
$$
x=\varphi(x),\qquad x_{k+1}=\varphi(x_k).
$$

**Достаточное условие (сжатие на отрезке):** существует отрезок $[a,b]$ такой, что  
1) $\varphi([a,b])\subseteq [a,b]$ 
2) $\varphi$ дифференцируема на $[a,b]$ и
$$
\max_{x\in[a,b]}|\varphi'(x)|=q<1.
$$



## 32. Достаточное условие сходимости метода релаксации (в виде $x=\varphi(x)=x-\lambda f(x)$)

Решаем $f(x)=0$ итерациями
$$
x_{k+1}=\varphi(x_k)=x_k-\lambda f(x_k).
$$

### Достаточное условие сходимости (через сжатие)
Пусть существует отрезок $[a,b]$ такой, что:

1) $\varphi([a,b])\subseteq [a,b]$, то есть
$$
a\le x-\lambda f(x)\le b \quad \forall x\in[a,b].
$$

2) $\varphi$ дифференцируема на $[a,b]$ и
$$
\max_{x\in[a,b]}|\varphi'(x)|=q<1.
$$

Так как
$$
\varphi'(x)=1-\lambda f'(x),
$$
то условие 2 эквивалентно
$$
\max_{x\in[a,b]}|1-\lambda f'(x)|<1.
$$
## 33. Метод итерации. Приведение уравнения к виду, пригодному для итерации

Исходное уравнение записывают как $f(x)=0$ и эквивалентно приводят к виду фиксированной точки
$$
x=\varphi(x),
$$
после чего применяют итерации
$$
x_{k+1}=\varphi(x_k),\quad k=0,1,2,\dots
$$

### Примеры преобразований $f(x)=0 \Rightarrow x=\varphi(x)$

1) **Выразить $x$ из части уравнения** (можно разными способами).  
Напр., для $x^3-2x-5=0$ можно взять:
$$
x=\frac{x^3-5}{2},\qquad
x=\sqrt[3]{2x+5},\qquad
x=\frac{2x+5}{x^2}\ (x\ne 0).
$$

2) **Добавление параметра $\lambda\ne 0$ (общий приём):**
$$
f(x)=0 \;\Longleftrightarrow\; x=x+\lambda f(x),
$$
то есть
$$
\varphi(x)=x+\lambda f(x).
$$
Напр., для $f(x)=x^3-2x-5$:
$$
\varphi(x)=x+\lambda(x^3-2x-5).
$$

3) **Метод Ньютона как частный случай итераций:**
$$
x_{n+1}=x_n-\frac{f(x_n)}{f'(x_n)}
\quad\Longleftrightarrow\quad
\varphi(x)=x-\frac{f(x)}{f'(x)}.
$$
(Это пример “адаптивного” выбора $\varphi$, зависящего от $f'(x)$.)


## 34. Функции MATHEMATICA для решения нелинейных уравнений

- `Solve[eqns, vars]` — точные решения уравнений/систем.
- `NSolve[eqns, vars]` — численные решения
- `FindRoot[eqn, {x, x0}]` — найти **один** корень (локально) по начальному приближению.
  - Для 1 переменной: `{x, x0}` — старт для Ньютона, `{x, x0, x1}` — вариант секущих.
- `Plot[f[x], {x, a, b}]` — пересечения $f(x)$ с осью $Ox$ дают корни.
- `Plot[{g[x], h[x]}, {x, a, b}]` — решения $g(x)=h(x)$ как точки пересечения графиков.

## 35. Численное дифференцирование. Простейшие формулы

Идея: разложить $f(x\pm h)$ в ряд Тейлора и отбросить малые члены.

### Как получается формула для $f'(x)$
$$
f(x+h)=f(x)+h f'(x)+O(h^2).
$$
Отсюда
$$
f'(x)\approx \frac{f(x+h)-f(x)}{h}\quad (O(h)).
$$
## 36. Формулы 2-го порядка точности (центральные)

Пусть значения $f$ заданы в узлах равномерной сетки $x_i=x_0+ih$ (шаг $h$).

### Что значит “центральная” формула
Центральная разностная формула использует симметричные точки вокруг $x_i$: $x_{i-1}=x_i-h$ и $x_{i+1}=x_i+h$.
Из‑за симметрии в разложении Тейлора сокращаются члены порядка $h$, поэтому точность получается второго порядка: $O(h^2)$.

### Первая производная (3 точки, $O(h^2)$)
$$
f'(x_i)\approx \frac{f(x_{i+1})-f(x_{i-1})}{2h}\quad (O(h^2)).
$$

### Вторая производная (3 точки, $O(h^2)$)
$$
f''(x_i)\approx \frac{f(x_{i+1})-2f(x_i)+f(x_{i-1})}{h^2}\quad (O(h^2)).
$$

## 37. Численное интегрирование. Прямоугольники, трапеции, параболы. Ньютон–Котес

Пусть нужно вычислить
$$
I=\int_a^b f(x)\,dx,\qquad x_i=a+ih,\ \ h=\frac{b-a}{n}.
$$

### Квадратурные формулы прямоугольников (степень 0)
Идея: на каждом отрезке заменить $f(x)$ константой.
- Левые прямоугольники:
$$
I\approx h\sum_{i=0}^{n-1} f(x_i).
$$
- Правые прямоугольники:
$$
I\approx h\sum_{i=1}^{n} f(x_i).
$$
- Средние прямоугольники:
$$
I\approx h\sum_{i=0}^{n-1} f\!\left(x_i+\frac{h}{2}\right).
$$

### Формула трапеций (степень 1)
Идея: на каждом $[x_i,x_{i+1}]$ заменить $f$ линейной функцией:
$$
I\approx \frac{h}{2}\Bigl(f(x_0)+2\sum_{i=1}^{n-1}f(x_i)+f(x_n)\Bigr).
$$

### Формула парабол (Симпсона) (степень 2)
Идея: на паре шагов аппроксимировать $f$ параболой (нужно чётное $n$):
$$
I\approx \frac{h}{3}\Bigl(f(x_0)+4\sum_{\substack{i=1\\ i\ \text{нечёт}}}^{n-1}f(x_i)
+2\sum_{\substack{i=2\\ i\ \text{чёт}}}^{n-2}f(x_i)+f(x_n)\Bigr).
$$

### Формулы Ньютона–Котеса
Это семейство формул вида
$$
\int_a^b f(x)\,dx \approx h\sum_{i=0}^{n} w_i\,f(x_i),
$$
где узлы $x_i$ равностоящие, а веса $w_i$ получаются интегрированием интерполяционного многочлена (частные случаи: прямоугольники, трапеции, Симпсон).

## 38????????. Квадратурные формулы Гаусса (Гаусса–Лежандра)

Идея: выбрать **неравномерные** узлы так, чтобы формула была максимально точной для многочленов.

### Формула на стандартном отрезке $[-1,1]$
$$
\int_{-1}^{1} f(x)\,dx \approx \sum_{i=1}^{n} w_i\,f(x_i),
$$
где узлы $x_i$ — корни полинома Лежандра $P_n(x)$, а веса $w_i$ подбираются соответствующим образом.

Главное свойство: $n$-точечная формула Гаусса–Лежандра **точна для всех многочленов степени до $2n-1$**.

### Переход к произвольному $[a,b]$
Делают замену
$$
x=\frac{a+b}{2}+\frac{b-a}{2}\,\xi,\quad \xi\in[-1,1],
$$
тогда
$$
\int_a^b f(x)\,dx=\frac{b-a}{2}\int_{-1}^{1} f\!\left(\frac{a+b}{2}+\frac{b-a}{2}\xi\right)d\xi
\approx \frac{b-a}{2}\sum_{i=1}^{n} w_i\,f\!\left(\frac{a+b}{2}+\frac{b-a}{2}x_i\right).
$$

## 39. Порядок убывания погрешности основных квадратурных формул
- Прямоугольников (левых/правых):  ошибка $O(h)$.
- Средних прямоугольников: ошибка $O(h^2)$.
- Трапеций: ошибка $O(h^2)$.
- Симпсона (парабол): ошибка $O(h^4)$ (нужно чётное $n$).
## 40-41. Алгебраическая точность квадратурных формул

Рассмотрим квадратурную формулу
$$
\int_a^b f(x)\,dx \approx \sum_{i=0}^{n} w_i\,f(x_i).
$$

### Определение
**Алгебраическая точность** равна наибольшему числу $m$, для которого формула интегрирует **точно** любой многочлен степени $\le m$,
но уже не точна для некоторого многочлена степени $m+1$.

### Примеры
- Средние прямоугольники и трапеции имеют алгебраическую точность $m=1$ (точны для констант и линейных функций).
- Формула Симпсона (парабол) имеет алгебраическую точность $m=3$.
- $n$-точечная формула Гаусса имеет алгебраическую точность $m=2n-1$.

## 42. Функции MATHEMATICA для интегрирования
- `Integrate[f, x]` — неопределённый интеграл.
- `Integrate[f, {x, a, b}]` — определённый интеграл.
- `NIntegrate[f, {x, a, b}]` — численное значение определённого интеграла.

## 43. Метод Эйлера и метод Эйлера–Коши (задача Коши)

Задача Коши:
$$
y'=f(x,y),\qquad y(x_0)=y_0,
$$
сетку берут $x_{k+1}=x_k+h$.

### Метод Эйлера
Идея: заменить производную конечной разностью, т.е. “идём по касательной” в точке $(x_k,y_k)$:
$$
y_{k+1}=y_k+h\,f(x_k,y_k).
$$
Точность метода: глобальная погрешность порядка $O(h)$.

### Метод Эйлера–Коши 
Идея: сначала сделать прогноз (Эйлер), потом уточнить, усреднив наклоны в начале и конце шага.

Прогноз:
$$
y_{k+1}^*=y_k+h\,f(x_k,y_k).
$$

Коррекция:
$$
y_{k+1}=y_k+\frac{h}{2}\Bigl(f(x_k,y_k)+f(x_{k+1},y_{k+1}^*)\Bigr).
$$

Точность: глобальная погрешность порядка $O(h^2)$.
## 44. Разница между явным и неявным Эйлером (задача Коши)

Задача Коши:
$$
y'=f(x,y),\quad y(x_0)=y_0,\quad x_{k+1}=x_k+h.
$$

### Явный (прямой) Эйлер
$$
y_{k+1}=y_k+h\,f(x_k,y_k).
$$
Тут правая часть зависит только от известных $x_k,y_k$, поэтому $y_{k+1}$ находится сразу.

### Неявный (обратный) Эйлер
$$
y_{k+1}=y_k+h\,f(x_{k+1},y_{k+1}).
$$
## 45. Метод Рунге–Кутта (задача Коши)

Задача Коши:
$$
y'=f(x,y),\quad y(x_0)=y_0,\quad x_{k+1}=x_k+h.
$$



### Классический Рунге–Кутта 4-го порядка (RK4)
$$
k_1=f(x_k,y_k),
$$
$$
k_2=f\!\left(x_k+\frac{h}{2},\,y_k+\frac{h}{2}k_1\right),
$$
$$
k_3=f\!\left(x_k+\frac{h}{2},\,y_k+\frac{h}{2}k_2\right),
$$
$$
k_4=f(x_k+h,\,y_k+h k_3),
$$
$$
y_{k+1}=y_k+\frac{h}{6}\,(k_1+2k_2+2k_3+k_4),
\qquad x_{k+1}=x_k+h.
$$

Точность: глобальная погрешность порядка $O(h^4)$.
## 46. Численные методы для ОДУ высших порядков

Если ОДУ имеет порядок $m$, то его **сводят к системе** из $m$ уравнений 1-го порядка и дальше решают как обычно (Эйлер, Эйлер–Коши, Рунге–Кутта).

### Сведение
Пусть
$$
y^{(m)}=F(x, y, y',\dots,y^{(m-1)}).
$$
Вводим:
$$
u_1=y,\quad u_2=y',\quad \dots,\quad u_m=y^{(m-1)}.
$$
Тогда система:
$$
u_1'=u_2,\quad u_2'=u_3,\ \dots,\ u_{m-1}'=u_m,\quad u_m'=F(x,u_1,\dots,u_m).
$$

### Дальше
К этой системе применяют любой метод для 1-го порядка (например, Эйлер или Рунге–Кутта) по шагам $x_{k+1}=x_k+h$.
## 47. Многошаговые методы. Разница с одношаговыми

Одношаговые: метод Эйлера, Эйлера-Коши, Рунге-Кутта
Многошаговые: метод Адамса
Многошаговые методы отличаются от одношаговых тем, что многошаговые для вычисления следующего значения используют более 1-го последнего предыдущего значения, в то время как одношаговые используют только последнее предыдущее значение.

## 48. Численные методы для задачи Коши систем ОДУ

Система векторно:
$$
\mathbf{y}'=\mathbf{f}(x,\mathbf{y}),\qquad \mathbf{y}(x_0)=\mathbf{y}_0,
$$
где $\mathbf{y}=(y_1,\dots,y_m)^T$.

### Главное правило
Почти любой метод для одного ОДУ переносится на систему: все формулы остаются теми же, только $y,\ f$ становятся **векторами**, а операции — покомпонентными.

### Пример: метод Эйлера
$$
\mathbf{y}_{k+1}=\mathbf{y}_k+h\,\mathbf{f}(x_k,\mathbf{y}_k).
$$

### Пример: Рунге–Кутта 4-го порядка (RK4)
$$
\mathbf{k}_1=\mathbf{f}(x_k,\mathbf{y}_k),
$$
$$
\mathbf{k}_2=\mathbf{f}\!\left(x_k+\frac{h}{2},\,\mathbf{y}_k+\frac{h}{2}\mathbf{k}_1\right),
$$
$$
\mathbf{k}_3=\mathbf{f}\!\left(x_k+\frac{h}{2},\,\mathbf{y}_k+\frac{h}{2}\mathbf{k}_2\right),
$$
$$
\mathbf{k}_4=\mathbf{f}(x_k+h,\,\mathbf{y}_k+h\mathbf{k}_3),
$$
$$
\mathbf{y}_{k+1}=\mathbf{y}_k+\frac{h}{6}\left(\mathbf{k}_1+2\mathbf{k}_2+2\mathbf{k}_3+\mathbf{k}_4\right).
$$

## 49. Функции MATHEMATICA для решения задачи Коши (ОДУ)
![alt text](image-2.png)